version: "3.9"

services:
    backend:
        build:
            context: ./backend
        ports:
            - "8080:8080"
        volumes:
            - ./backend/app:/backend/app
        command:
            sh -c "python manage.py runserver 0.0.0.0:8000"